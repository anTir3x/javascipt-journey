// Setup
let sum = 0;

function addThree() {
  sum = sum + 3;
  return sum;
}

// Only change code below this line
function addFive(){
   sum = sum + 5; 
   return sum;
}

// Only change code above this line

addThree();
console.log(addThree());
addFive();
console.log(addFive());
